# V3 迁移记录

> 状态：已完成

## 范围

把 `local-ops-console` 作为第一个真实复杂项目迁入标准项目根目录，并验证零上下文接手、m-wiki 注册和 OpenWiki 候选流程。

## 原始现场

- 旧路径：`/Users/arvin/Documents/Codex/projects/local-ops/local-ops-console`
- 旧提交：`d3a9ddaa2a7ae9b3fffc922741a60e5a58f2d4ef`
- 分支：`main`
- 迁移前 Git：干净
- 基线：`npm run check` 通过，`npm test` 90/90 通过

## 迁移策略

1. 只读检查旧目录、Git、运行引用、特殊目录和大文件。
2. 使用 APFS 克隆复制到 `/Users/arvin/Documents/AI/codex/local-ops-console`。
3. 不移动、不删除旧目录，不修改正在运行的安装副本。
4. 在新副本增加标准项目文档，验证后再注册 m-wiki。
5. OpenWiki 只生成隔离候选，审阅通过后才接受。

## 完成结果

- 标准化副本通过 `project-foundation check`。
- m-wiki 已登记 `local-ops-console`，入口为可重建链接，不复制项目文档。
- OpenWiki 初始候选 `20260809T055433524456` 已完成链接、秘密、范围和源码事实抽查后接受。
- `.openwikiignore` 末尾格式修正使输入哈希变化 1 字节；没有手改状态，重新生成并审阅候选 `20260809T062514831109` 后显式接受。
- 接受后的 OpenWiki 代码哈希检查为 `no-op`。
- 新副本 `npm run check`、90 项单元测试与 `git diff --check` 均通过。
- 仅迁移源码和文档；没有安装、升级、停止或重启 Local Ops 运行时。

## 恢复方法

如果任一门禁失败，停止使用新副本，保留失败现场供诊断，继续从旧路径和旧提交工作。旧目录在用户确认迁移稳定前不得删除。

- 旧路径与旧提交：`/Users/arvin/Documents/Codex/projects/local-ops/local-ops-console` @ `d3a9ddaa2a7ae9b3fffc922741a60e5a58f2d4ef`。
- 最终 OpenWiki 接受前备份：`/Users/arvin/.local/state/project-foundation/backups/local-ops-console/openwiki-20260809T063200500964`。
