# 临时文档

这里保存实施计划、调研草稿、重构方案、临时问题分析和尚未批准的设计。

规则：

- 文件名优先使用 `YYYY-MM-DD-主题.md`。
- 开头标明：草稿、执行中、已完成或已废弃。
- 文件正常进入 Git，供跨任务追溯。
- 稳定结论迁移到 `usage/`、`runbooks/`、`decisions/` 或 `pitfalls/`。
- OpenWiki 必须完全忽略本目录，不得读取、总结或引用。
